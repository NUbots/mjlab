apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mjlab-workspace
  namespace: mjlab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-local
  resources:
    requests:
      storage: 50Gi
